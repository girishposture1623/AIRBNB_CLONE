<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Create Listing</title>
    <link rel="stylesheet" href="../styles/form.css" />
  </head>
  <body>
    <h1>Create New Listing</h1>

    <form method="post" enctype="multipart/form-data">
      <fieldset>
        <legend>Basic Information</legend>

        <label>Title</label>
        <input type="text" name="title" required />

        <label>Description</label>
        <textarea name="description" rows="4" required></textarea>
         <label>rent</label>
        <input name="rent" type="text" rows="4" required></input>
         <label>city</label>
        <input name="city" type="text" rows="4" required></input>
         <label>landmark</label>
        <input name="landmark" type="text" rows="4" required></input>
         <label>cateogery</label>
        <input name="cateogery" type="text" rows="4" required></input>
      </fieldset>

      <!-- PROPERTY DETAILS -->
      <!-- <fieldset>
        <legend>Property Details</legend>
        <div class="property-grid">
          <div>
            <label>Guests</label>
            <input type="number" name="guests" min="1" required />
          </div>
          <div>
            <label>Bedrooms</label>
            <input type="number" name="bedrooms" min="0" required />
          </div>
          <div>
            <label>Beds</label>
            <input type="number" name="beds" min="0" required />
          </div>
          <div>
            <label>Bathrooms</label>
            <input type="number" name="bathrooms" min="0" step="0.5" required />
          </div>
        </div>
      </fieldset> -->
      <fieldset class="image-upload-group">
        <!-- <legend>Property Images</legend> -->
        <!-- <label>Upload 5 high-quality photos</label> -->

        <input type="file" name="photo" accept="image/*" required />

        <!-- <small>Supported formats: JPG, PNG, AVIF</small> -->
      </fieldset>
     

      <button type="submit">Create Listing</button>
    </form>

   <!-- <div>
  <% Data.forEach(item => { %>
    <p><%= item.title %></p>
    <img src="<%= item.photo %>" width="200">
  <% }) %>
</div> -->

  </body>
</html>
